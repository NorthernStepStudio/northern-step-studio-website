# Verification Documentation

Verification behavior is driven by:

- `studioos/validation/verification-profile.json`

Risk-driven defaults:

- `low`: hygiene checks
- `medium`: hygiene + project checks
- `high` and `critical`: hygiene + checks + test suite

Verification must run before deployment decisions.

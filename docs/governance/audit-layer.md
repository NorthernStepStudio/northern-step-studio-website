# Audit Layer

Each governance run writes immutable evidence:

- JSON audit: `studioos/audits/`
- markdown summary: `studioos/reports/`

Audit entries include:

- policy decisions
- verification results
- control violations
- artifact references

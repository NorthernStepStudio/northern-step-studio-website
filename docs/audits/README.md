# Audit Documentation

This folder defines audit interpretation standards for StudioOS governance outputs.

Primary machine records are written to:

- `studioos/audits/` (JSON run records)
- `studioos/reports/` (human-readable summaries)

Audit records must include:

- change id
- timestamp
- risk class
- approval status
- verification results
- control violations
- recovery artifact reference

# Synox Core Package

This package path is currently `packages/synox` for compatibility, but its canonical internal name is **Synox**.

## Product copy
- Matterhorn is the main NStep AI agent.
- Synox is the core intelligence engine powering Matterhorn.
- NStep AI is the broader product and ecosystem.

## Branding notes
- Use `Matterhorn` for user-facing UI labels.
- Use `Synox` for core runtime and provider references.
- Keep `synox` import/path compatibility until a dedicated package rename is scheduled.
export const SelectionState = {
  activePartId: null as string | null,
  activeAnimId: 'idle' as string,
  clipboardController: null as any | null,
  isEditingPivot: false,
  showDebugBounds: false,
};

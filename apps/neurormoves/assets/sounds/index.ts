// Placeholder sound files - these will be replaced with actual audio files
// For now, the GameContext will fallback to haptic feedback if files don't exist

export const SOUND_ASSETS = {
    success: require('./success.mp3'),
    error: require('./error.mp3'),
    pop: require('./pop.mp3'),
};

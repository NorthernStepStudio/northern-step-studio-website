This folder replaces the legacy `vendor/response-os` directory.
It is intentionally empty in the working tree; vendor assets can be populated from the archive or package kit as needed.

Rename notes:
- Legacy path: `vendor/response-os`
- New path: `vendor/synox`

Do not commit large generated build artifacts here; prefer keeping source and packaging scripts in the main repo.

import { aggregateAssistantContext, formatContextForPrompt } from "./src/worker/synox/contextAggregator.ts";
// This is a test script, it won't work directly because of imports and Env.
// I'll create a standalone test that mock the DB.
